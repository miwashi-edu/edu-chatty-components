import React from 'react';
//import {withConfig, withCookies, withAuth, withApi} from '../../decorators';
import {Header} from '.';

export default {
    title: 'Components/Header',
    component: Header,
}

export const Default = {

}